# Docker Hub credentials for production deployments
# Only needed if deploying from docker-compose (not GitHub Actions)

DOCKERHUB_USERNAME=gashmurble
TARGET_ENV=prod

# These images will be used in docker-compose.prod.yml:
# ${DOCKERHUB_USERNAME}/digitmile-game:${TARGET_ENV}-latest
# ${DOCKERHUB_USERNAME}/digitmile-backend:${TARGET_ENV}-latest
