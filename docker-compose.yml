version: '3.8'

services:
  # PostgreSQL Database
  db:
    image: postgres:16-alpine
    container_name: digitmile-postgres
    env_file:
      - .env
    environment:
      # PostgreSQL uses these specific env vars to create the initial user/database
      POSTGRES_DB: ${DB_NAME:-digitmile}
      POSTGRES_USER: ${DB_USER:-digitmile}
      POSTGRES_PASSWORD: ${DB_PASS:-changeme}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    networks:
      - digitmile-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U digitmile -d digitmile"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Django Backend (DigitMilePanel)
  backend:
    build:
      context: ./DigitMilePanel
      dockerfile: Dockerfile.compose
    container_name: digitmile-backend
    env_file:
      - .env
    volumes:
      # Mount code for development (changes reflect immediately without rebuild)
      - ./DigitMilePanel:/app
    ports:
      - "8000:8000"
    depends_on:
      db:
        condition: service_healthy
    networks:
      - digitmile-network
    command: >
      sh -c "python manage.py migrate &&
             python manage.py collectstatic --noinput &&
             python manage.py create_superuser &&
             gunicorn digitmile.wsgi:application --bind 0.0.0.0:8000 --workers 3"

  # Nginx Frontend (DigitMile Game)
  frontend:
    build:
      context: ./DigitMile
      dockerfile: Dockerfile
    container_name: digitmile-game
    ports:
      - "80:80"
    depends_on:
      - backend
    networks:
      - digitmile-network

networks:
  digitmile-network:
    driver: bridge

volumes:
  postgres_data:
